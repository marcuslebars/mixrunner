# Core dependencies (Cross-platform compatible)
numpy>=1.26.0
scipy>=1.11.0
pyyaml>=6.0

# Audio analysis
librosa>=0.10.0
soundfile>=0.12.0
pyloudnorm>=0.1.0
audioread>=3.0.0

# AI/ML for track classification
scikit-learn>=1.3.0

# Audio processing
pydub>=0.25.0

# Utilities
tqdm>=4.65.0
colorama>=0.4.6
click>=8.1.0
loguru>=0.7.0
dataclasses-json>=0.6.0

# Note: pyobjc packages are macOS-only and removed for Windows compatibility
# When running on macOS, Logic Pro integration will work automatically

# LLM Integration (for AI-powered mixing)
anthropic>=0.18.0  # Claude API
openai>=1.12.0     # GPT API
google-generativeai>=0.3.0  # Gemini API
